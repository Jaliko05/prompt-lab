# API Configuration
VITE_API_BASE_URL=http://localhost:5000

# JWT Token para autenticación en /api/*
# Este token se envía automáticamente en el header 'Authorization: Bearer <token>'
VITE_JWT_TOKEN=tu_jwt_token_aqui

# Admin API Key para endpoints de administración /admin/*
# Esta clave se envía en el header 'X-Admin-API-Key' 
VITE_ADMIN_API_KEY=tu_admin_api_key_aqui
